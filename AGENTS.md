# AGENTS

本文件定义 astrolabe 项目的项目级代理协作约定。进入本仓库工作时，默认先读取并遵守这里的规则。

## 项目使命

astrolabe 用于构建可靠、可复现、高性能的数据处理、回测和因子计算/研究基础设施。系统应支持数据接入与清洗、标准化与质量检查、历史数据存储与查询、批量及增量因子计算、事件驱动或向量化回测、组合与绩效分析，以及实验记录、benchmark 和确定性 replay。

正确的数据时间语义、无前视偏差、数值与账户状态一致性、研究可复现性是硬约束。在此基础上，按任务优化处理吞吐、计算耗时、内存占用和增量计算延迟。不得以性能为由改变数据、因子或回测语义，也不得将回测表现等同于未来实盘收益。

## 默认约定

- 默认使用中文回答，除非用户明确要求其他语言。
- 文档使用中文撰写；项目自有 Python 和 C++ 代码中的注释、docstring 使用英文；commit message 使用英文。
- 专业名词、协议字段、配置项、命令、路径、代码标识符、日志原文和外部 API 名称保留英文。
- 项目语言为 Python 和 C++；C++ 技术基线与 astra 一致，为 Linux、CMake 和 C++20。Python 版本与工具链以仓库实际配置为准；尚未建立时，在首次实现中明确最低支持版本和可复现环境。
- Python 承担数据流程编排、研究接口、实验分析和参考实现；C++ 用于经测量确认需要的性能关键计算与引擎组件。具体模块边界由需求与证据决定，不为使用某种语言而重复实现。
- 不把某个 dataframe、存储、任务调度或 Python/C++ binding 框架视为既定架构；引入前说明需求、兼容性和维护成本。
- 保持改动最小且可回滚，不做与当前任务无关的重构。
- 不根据直觉宣称性能、并发安全、因子有效性或策略收益；结论必须有 fresh benchmark、profile、replay、测试或实验记录。
- 不把账号、API key、secret、private key、session token、完整账户信息或受限原始数据写入代码、日志、测试 fixture 和提交。
- 临时日志、profile、benchmark 输出和 scratch 数据写入源码树之外的临时目录，例如 `${TMPDIR:-/tmp}/astrolabe`；需要长期保留的研究产物写入显式配置的产物目录，并记录路径。
- 除非用户明确要求，不自动 commit、push、创建 PR、部署或执行不可逆的外部操作。本仓库默认开展离线数据与研究任务，不发送真实订单或修改真实账户。

## 新对话启动

进入仓库后先执行：

- `git status --short --branch`
- `git log --oneline -8`

随后按存在性读取：

- `AGENTS.md`
- `README.md`
- `docs/` 下与当前任务直接相关的设计、数据 contract、研究方法和 handoff 文档
- 当前模块的 Python 项目配置、`CMakeLists.txt`、测试与 benchmark 入口

如果工作区存在未提交改动，先识别所有者和意图，只修改本任务拥有的文件或 hunk。仓库尚未建立的命令、目录和依赖不得描述为已经存在。

## Required skills 与任务分级

本项目使用 adaptive-development，为实现、测试、调试、性能优化和长期技术目标选择适当的流程强度。该 skill 不可用时明确说明，并按本文件执行适当的质量门。

涉及编码、构建、测试、性能或运行配置时，先输出：

`Level: L0|L1|L2|L3 — <一句判断依据>. Workflow: <本轮质量门>.`

级别定义：

- L0：只读解释、调查、诊断或 review；不修改文件。
- L1：无预期行为变化的机械修改；执行最小 diff review 和验证。
- L2：边界明确、可验证、易回滚的局部行为修改；先建立失败测试或最小复现，再实现、review 和 focused verification。纯文档修改通过内容与一致性检查验证。
- L3：高影响、难回滚、跨模块或长期有效的设计与实现；使用隔离 branch/worktree、书面计划、独立 review 和完整风险验证。提交遵循本文件的 Git 与交付约定。

下列任务默认至少为 L3：

- 修改数据 schema、时间对齐、交易日历、复权、合约映射、point-in-time 查询或 persistent format。
- 修改回测事件排序、撮合、成交价格、费用、现金、持仓、保证金、结算或收益核算语义。
- 修改因子窗口、可用时点、横截面样本、训练/验证切分或其他影响前视偏差的 contract。
- 修改 Python/C++ 边界、对外 ABI、线程/进程模型、对象所有权、同步方式或 hot path。
- 声明或追求性能改善、因子有效性或策略收益提升。
- 引入长期架构、跨模块 contract、大规模历史数据迁移、分布式计算、GPU 或硬件依赖。

设计讨论先调查仓库事实并明确关键取舍。仅在缺失信息会实质改变设计、研究结论或外部操作授权时请求用户决策；已获授权且边界明确的工作继续推进。不要求尚未安装的访谈 skill 作为常规工作前置条件。

## 数据 contract 与时间语义

- 每份数据明确来源、schema 版本、主键、排序规则、字段类型、单位、精度、时区、交易日历和缺失值语义。
- 区分事件发生时间、交易所时间、发布时间、接收/入库时间和研究可用时间；历史修订还须记录版本及修订可用时间。不得用事件日期替代真实可获知时点。
- point-in-time 查询和 as-of join 只允许读取决策时点已经可用的数据版本；明确相同时间戳的可见性和稳定排序规则。
- 时间戳使用明确精度和时区的表示；统一存储时保留交易所时区与交易日映射。显式处理跨午夜交易、夜盘、节假日和 DST。
- 明确区分缺失、停牌、无成交、零值、NaN、Inf 和无效记录。插值、forward fill、去极值、过滤和去重必须有适用边界与审计信息，禁止静默修补。
- 外部文件和输入视为不可信：验证长度、类型、范围、枚举、压缩边界和记录完整性。重复、乱序、缺口、迟到和修订数据必须有确定行为。
- 原始数据保留可追溯版本；清洗、聚合、复权和衍生结果与原始数据分层管理，不原地覆盖唯一原始副本。
- 股票池与标的元数据必须保留历史状态，覆盖退市、停牌、上市日期、成分调整和公司行动，避免幸存者偏差。
- 前复权/后复权、分红拆股、期货换月与连续合约拼接规则必须明确，并验证是否使用未来已知信息。研究价格序列与可成交合约价格不得混用。
- tick size、lot size、contract multiplier、币种、汇率和精度元数据按生效时间管理并在使用前校验。
- 数据写入应具备明确的原子发布、幂等、失败重试和恢复语义；分区重算不得产生重复记录、混合版本或半成品可见状态。
- 大数据集与运行产物不直接提交到 Git；测试使用小型、可分发、确定性的合成或脱敏 fixture，数据快照通过版本或 manifest 引用。

## 因子计算与研究

- 每个因子定义输入字段、频率、历史窗口、最小样本数、窗口端点、输出时间、可用时点、缺失值处理和参数。
- 明确计算属于时间序列、横截面或两者组合；横截面处理只能使用当时可用的样本池及数据。
- rolling、lag、resample 和 join 必须显式定义标签、闭开区间与对齐方向；禁止通过全样本统计量或未来数据填充历史结果。
- 标准化、去极值、中性化、特征选择和模型拟合遵循训练/验证边界；学习得到的参数只能来自允许的历史样本。同期横截面变换须说明数据可用性。
- 批量与增量实现共享同一语义，明确 warm-up、状态 checkpoint、迟到数据和历史修订后的重算范围；使用 differential test 对齐结果。
- 缓存键至少覆盖输入数据版本、代码/因子版本、参数、时间范围、样本池和影响结果的配置；不得复用语义不一致的缓存。
- 标签和未来收益与特征计算隔离，明确预测区间、持有期和重叠标签；时间切分按需要使用 purge/embargo，禁止将普通随机切分默认用于存在时间依赖的样本。
- 因子研究按适用范围报告 IC/Rank IC、分组收益、多空收益、换手、覆盖率、稳定性及成本敏感性，并说明样本量、样本期和统计方法。
- 区分训练、验证与最终样本外结果；记录参数搜索范围和试验次数，防止反复使用测试集调参以及选择性展示结果。
- 因子有效性结论应说明缺失数据、样本选择、交易成本和多重检验的影响；结果不足时明确为 inconclusive。

## 回测与账户核算

- 明确引擎是事件驱动还是向量化，以及两者支持的语义边界；不得在不满足成交与状态假设时互换结果。
- 定义事件时钟和同一时间戳内的处理顺序，覆盖行情、信号、下单、撮合、成交、费用和结算。历史推进使用模拟事件时间；运行耗时使用 monotonic clock。
- 信号只能使用决策时点可用的信息。使用完整 bar 生成的信号，不得默认以该 bar 内此前已发生的价格成交；收盘成交等特殊模型必须说明可执行条件。
- 撮合模型显式定义订单类型、价格限制、成交量约束、部分成交、撤单、拒单、过期、延迟和排队假设。仅有 OHLC 数据时，不臆造真实 bar 内路径。
- 模拟订单状态机覆盖实现所支持的生命周期，并保持订单、成交和持仓更新一致；重复事件、重放与恢复不得重复计费或记账。
- 手续费、滑点、价差、冲击、税费、借券成本、融资成本和资金费率按适用市场建模；未建模项及默认值必须出现在结果假设中。
- 区分现金、可用资金、冻结资金、持仓、可卖数量、保证金、已实现/未实现 PnL 和权益；明确多币种估值、乘数、结算、分红、拆股和到期处理。
- 价格、数量及资金/订单逻辑遵循 astra 的精度要求：使用定点整数或经过审查的 decimal 表示，不依赖二进制浮点近似。研究特征和统计可使用浮点，但须定义精度与误差容忍度。
- 明确仓位限制、杠杆、保证金、做空可用性、涨跌停、停牌和交易时段等约束。数据缺失、估值失效或资金不足不得悄悄产生有利成交。
- 对成交、费用、现金和持仓建立可逐事件核对的账本；以权益恒等式及适用的账户不变量验证核算。
- 明确收益、复利、年化、波动率、Sharpe、回撤和换手的公式、采样频率与基准；不得用不同口径直接比较策略。
- 基线回测应可确定性重放；随机撮合或蒙特卡洛模型必须显式启用并记录随机种子及流分配。回测结果不作为自动执行真实交易的授权。

## Python 编码与 Python/C++ 边界

- 遵循 PEP 8；格式化、lint 和类型检查以仓库配置为准。公共接口、跨模块数据结构和关键数值接口提供明确的类型注解。
- 研究 notebook 用于探索与展示，可复用逻辑进入可测试模块；可交付 notebook 必须支持从干净内核顺序执行，不依赖隐藏状态或执行顺序。
- 避免隐式全局状态、import 时 I/O、隐式联网下载和未受控的随机源。数据路径、参数和执行入口显式配置。
- 优先清晰的向量化或分块处理；避免无意的逐行 Python 循环、大对象复制和一次性全量载入。性能选择必须有测量依据。
- 明确 dataframe/array 的索引对齐、排序、dtype、nullable 语义和 copy/view 行为；避免依赖库的隐式类型转换。
- 对外部资源使用 context manager；异常提供可定位的上下文，不以宽泛捕获后返回空表、零值或部分结果掩盖失败。
- Python 与 C++ 接口明确 dtype、shape、stride、连续性、alignment、空输入、所有权和生命周期；校验完成后才进入可信计算路径。
- zero-copy 必须证明底层 buffer 在整个调用或异步任务期间有效，且无不受控并发修改；不能以减少复制为由保留悬空引用。
- 长时间纯 C++ 计算可按 binding 机制释放 GIL，但不得在未持有 GIL 时访问要求 GIL 的 Python API；异常必须在边界转换，不能穿越不支持异常的 ABI。
- 明确 Python workers、C++ threads 和 BLAS/OpenMP 的线程预算，避免嵌套并行造成 oversubscription；不要假设 GIL 能保护原生线程共享状态。
- 依赖在仓库配置中声明并采用可复现的版本约束或锁定机制，不依赖开发机上未声明的包。

## C++ 编码规则

以下要求与 astra 保持一致，适用于项目自有 C++ 代码；Python 研究代码不机械套用 C++ hot path 限制。

- 项目自有 C++ 命名遵循 Google C++ Style Guide 的 Naming 章节；机械格式遵循根目录 `.clang-format`。建立 C++ 工具链时采用 astra 的格式配置；该文件尚未存在时不得声称已通过格式检查。
- 使用 RAII 表达资源所有权；裸指针默认表示 non-owning，所有权转移使用明确类型。
- hot path 不抛异常；边界层如使用异常，必须在进入核心计算/回测主路径前终止传播并转换为显式错误。
- 避免 `shared_ptr` 进入 hot path；优先唯一所有权、稳定生命周期或只读引用。
- 避免隐式窄化、signed/unsigned 混用、未对齐访问、strict-aliasing 违规和对象生命周期 UB。
- 新依赖必须说明功能、性能、许可证、升级和故障边界；能用小而清晰的自有实现时不引入重量级框架。
- 第三方与生成代码保持上游风格，不做无关格式化。
- 编译警告视为需要处理的问题；局部抑制必须有理由，不能全局关闭重要诊断。

## Hot path 规则

hot path 应由 profile 或明确的计算预算确认。在已确认的 C++ hot path 中，沿用 astra 的要求，默认避免：

- 动态分配、引用计数、异常、RTTI 和不可控析构链。
- 阻塞锁、条件变量、系统调用、文件 I/O、DNS、同步日志和字符串格式化。
- 不必要的虚函数、间接跳转、分支、边界检查、复制、序列化和容器查找。
- 隐式初始化、lazy initialization、首次访问 page fault 和不可预测的 cache miss。
- 无界队列、无界重试、隐式 backpressure 和过度 batching。

优先考虑：

- 启动阶段预分配、预热、预计算并锁定稳定配置。
- 明确所有权、固定容量的数据结构、连续内存、cache-friendly layout 和少量稳定分支。
- 单写者设计、SPSC 通道、每核分片和避免 false sharing 的 cache-line 对齐。
- 编译期多态、组合或 CRTP；只有测量证明可忽略时才保留 hot path 动态分发。
- 就地解析与就地构造；采用 zero-copy 前验证生命周期、别名和边界安全。
- 将审计、持久化、非关键日志及与目标无关的统计移出关键计算路径。
- 批处理明确容量和内存预算；增量低延迟路径还须限定最大等待时间并验证尾延迟。离线批任务按吞吐与内存目标测量。

不允许以性能为理由引入未定义行为、越界访问、数据竞争、未校验输入或错误研究结果。必要的输入校验可以前移到边界，但不能省略。

## CPU、内存、NUMA 与并发

- 对性能关键线程明确 CPU affinity、NUMA 归属和跨核通信拓扑；涉及网络接入时同时说明 IRQ/RPS/XPS 关系。
- 每个可变状态应有唯一且清晰的 owner；跨线程状态优先通过显式消息传递。
- 共享数据必须记录读写线程、所有权、同步原语和 memory order；禁止凭 x86 偶然行为推断可移植正确性。
- 使用 relaxed memory order 时必须说明 happens-before 关系，并有并发测试或模型依据。
- 对 cache line 竞争、false sharing、TLB miss、page fault 和 remote NUMA access 保持敏感。
- huge pages、mlock、prefault、isolcpus、nohz_full、rcu_nocbs、busy polling 等设置只能在有测量和部署文档时引入。
- 结构体压缩、SoA/AoS、prefetch 和 branch hint 都必须以 profile 或 benchmark 为依据。
- 不在 hot path 中使用通用 allocator；自定义 pool/arena 必须定义容量耗尽、回收、ABA 和跨线程语义。
- 队列必须有界，并定义 full/empty、drop、retry、backpressure、shutdown 和 recovery 语义；多生产者/多消费者结构只有在需求和 benchmark 证明必要时使用。
- 研究输入、回测订单、成交与账户事件默认不得丢弃。允许 coalescing、采样或近似计算时必须显式定义用途、误差和结果标记，不得悄悄改变基线语义。
- 资源耗尽必须停止发布结果或进入明确且可观测的 degraded mode，不能静默覆盖数据或把部分产物标记为成功。
- shutdown、任务取消和 worker 失败必须可测试；恢复时明确已完成分区、状态 checkpoint 和重复执行边界。
- 并发修改至少运行 focused stress test；适用时运行 TSAN，但不要把 TSAN 数值当作性能结果。

## 性能目标与测量纪律

- 先定义测量边界：读取/解压、解析/清洗、join/聚合、因子 kernel、完整因子流水线、回测事件循环、Python/C++ 调用或 end-to-end。
- 批量任务报告数据规模、总耗时、吞吐、峰值内存及适用的 I/O 指标；增量或延迟敏感任务同时报告 median、p95、p99、p99.9、max 和样本数，不能只报告平均值。
- benchmark 记录 CPU 型号、核心绑定、NUMA node、SMT、频率策略、编译器与编译参数、Python 与主要依赖版本、线程数、数据规模、warm-up 和运行时长。
- 使用稳定、可解释的时钟源；跨核使用 TSC 前验证 invariant/synchronized TSC，明确转换和校准方法。
- 区分 cold start、warm path 和 steady state；记录 OS page cache、数据缓存和编译/JIT warm-up 条件，不得用 warm cache 数值冒充首次运行表现。
- 性能测试隔离日志、监控、debug assertions、sanitizer 和其他噪声源，同时保留单独的正确性构建。
- benchmark 结果必须可复现，保存命令、配置、输入版本和原始输出位置。
- 任何优化都要与基线在同机、同配置、同负载下 A/B 对比，并通过 reference/differential test 验证行为一致。
- SIMD、并行归约、fast-math 和不同浮点 dtype 可能改变数值结果；采用前明确误差预算、NaN/Inf 行为和确定性要求。
- 若噪声大于预期收益，结论应为 inconclusive，而不是“优化成功”。

## 可复现性与可观测性

- 每次可交付研究或回测记录 run id、代码 commit（含 dirty 状态或补丁标识）、数据版本/manifest、配置、参数、环境、随机种子和产物位置。
- 记录起止区间、交易日历、标的池、benchmark、成本模型与关键假设，使结果能够从同一输入重新生成。
- 并行随机数流按任务稳定分配，不依赖调度顺序；浮点并行归约无法逐位复现时，说明容忍度及适用平台。
- 指标使用稳定名称与单位，至少覆盖输入/输出行数、过滤与缺失数量、重复/乱序/迟到数据、各阶段耗时、内存、queue high-water mark、重试和失败分区。
- 回测日志应能定位订单、成交、费用和账户状态变化；异常结果能追溯到输入记录和计算步骤。
- 可观测性不能破坏关键路径；按需要使用固定结构事件、per-thread buffer、采样和异步消费。日志不得包含 secret 或完整敏感数据。
- 诊断字段或报告 schema 改动时同步更新文档、消费者与兼容策略；区分数据失败、计算失败、资源耗尽和结果校验失败。

## 测试与验证

行为修改根据风险选择以下证据：

- unit test：解析、时间对齐、窗口边界、定点运算、状态转换、缺失值和错误处理。
- property/fuzz test：异常输入、数值范围、时间序列组合、账户不变量和序列化往返。
- point-in-time test：改变或追加未来数据不得改变此前已经可用的因子与决策；覆盖历史修订、成分变更和财报发布时间。
- replay/golden test：小型行情、订单、成交及账户序列的确定性重放，明确预期费用、持仓和权益。
- differential test：C++ 与清晰 Python 参考实现、优化前后实现、批量与增量计算逐记录或逐事件对比；浮点比较使用明确的绝对/相对容忍度。
- integration test：读取、清洗、因子、回测及报告的最小端到端流程，覆盖 Python/C++ 边界。
- stress/concurrency test：队列容量、worker 失败、取消、shutdown 和重复执行。
- sanitizer build：ASAN、UBSAN、TSAN 按适用范围分别运行。
- benchmark/profile：目标路径的吞吐、峰值内存、延迟分布、CPU cycles、cache/branch 指标和端到端测量。
- fault injection/recovery test：损坏分区、输入缺口、迟到修订、磁盘/队列压力、中断和 checkpoint 恢复，验证无重复、无半成品发布。

修复缺陷时优先先建立能失败的最小复现或回归测试。验证命令必须来自仓库已有构建入口；若入口尚未建立，先在 `README.md` 或 `docs/` 中定义最小可重复流程。纯文档改动不要求构建无关代码，但必须检查内容一致性与 `git diff --check`。

## Review 检查表

每次修改按适用范围检查：

- 是否改变数据 schema、时间、精度、样本池、因子窗口、撮合、账户或持久化 contract。
- 是否引入前视偏差、幸存者偏差、训练/测试泄漏、过拟合或成本遗漏。
- 是否处理缺失、NaN/Inf、重复、乱序、迟到、历史修订和空输入。
- Python/C++ 边界的类型、所有权、GIL、buffer 生命周期和异常传播是否明确。
- hot path 是否新增分配、锁、系统调用、日志、格式化、复制或不可预测分支。
- 是否产生无界队列、无界重试、silent drop、data race、错误 memory order 或不明确的 overload 行为。
- 批量/增量及参考/优化实现是否保持一致，账户不变量与随机性是否得到验证。
- benchmark 是否与基线同条件、覆盖目标指标并保存原始证据；研究结果是否记录样本外表现与假设。
- 文档、配置、数据版本、指标和测试是否与实现同步。
- diff 是否只包含本任务文件，是否保留用户已有修改。

## Git 与交付

- 提交保持原子性，不裹带无关改动。
- 提交前至少运行 `git diff --check` 和与改动风险匹配的验证。
- 没有 fresh 验证证据时，不宣称实现、性能目标或研究结论已得到验证。
- 用户未要求 commit 时，保留改动供 review；用户要求 commit 时使用英文 commit message。
- push、PR、部署、受限数据发布和真实交易操作需要用户明确授权；skill 默认交付流程不得覆盖本约定。
- 最终回复报告修改内容、验证命令与结果，以及实际存在的未覆盖风险或阻塞。

## 一句话原则

数据与时间语义正确、回测可信、研究可复现是硬约束；在此基础上，用可复现证据提高计算效率与研究质量。
